import "tfplan"
import "version"

# This regular expression checks whether the Terraform version is greater then the

param tfversion default "1.3.0"

main = rule {
  version.new(tfplan.terraform_version).greater_than(tfversion)
}

